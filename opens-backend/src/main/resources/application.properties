opens.jwtSecret = ======================BezKoder=Spring=================================================BezKoder=Spring===========================
opens.jwtExpirationMs = 900000


spring.datasource.url=jdbc:postgresql://localhost:5432/opens
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# This will create table automatically in your database
spring.jpa.hibernate.ddl-auto=update

spring.jpa.properties.hibernate.jdbc.time_zone=CET

# ignore missing fonts in reports
net.sf.jasperreports.awt.ignore.missing.font=false